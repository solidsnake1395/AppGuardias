{
  "name": "fakeapirest",
  "version": "1.0.0",
  "description": "Backend falso con JSON Server",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
